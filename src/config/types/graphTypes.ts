export type DiagramData = {
  date: Date;
  close: number;
};

export type PieChartData = {
  label: string;
  value: number;
};

export type GraphDimensions = {
  width: number;
  height: number;
};
